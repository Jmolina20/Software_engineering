var letter='B';
letter = letter.toLowerCase();
if (letter == 'a' || letter == 'e' || letter=='i' || letter=='o' || letter=='u'){
    console.log ("The character contained within the variable is a vowel, it's: " + letter);
}
else {
    console.log("Nope, sorry you've got a consonant and it's " + letter);
}